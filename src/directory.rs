/// A Lis directory
pub struct Diectory {
    full_path: PathBuf,
    doc_id: String,
}

impl Directory {
    pub fn new(full_path: &Path, root_doc_id: String) -> Self {
        // check if directory doesn't already exist
        DirTree {}
    }
}
